# Server configuration
PORT=3001
BASE_URL=http://localhost:3001
# CLIENT_URL should point to the base URL (OAuth will redirect to /multiplayer.html)
CLIENT_URL=http://localhost:5173

# Authentication
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Discord OAuth (get credentials from https://discord.com/developers/applications)
DISCORD_CLIENT_ID=your-discord-client-id
DISCORD_CLIENT_SECRET=your-discord-client-secret

# Discord Activity (same client ID for embedded app SDK)
VITE_DISCORD_CLIENT_ID=your-discord-client-id

# Google OAuth (get credentials from https://console.cloud.google.com/apis/credentials)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Facebook OAuth (get credentials from https://developers.facebook.com/apps/)
FACEBOOK_APP_ID=your-facebook-app-id
FACEBOOK_APP_SECRET=your-facebook-app-secret

# Client configuration (for Vite)
# For development:
VITE_SERVER_URL=http://localhost:3001

# For production, create a .env.production file with:
# VITE_SERVER_URL=https://your-production-server.com
# Or leave it unset to auto-detect based on the page's protocol and hostname

